---
id: F-0186
title: 主车数量
level: feature              # capability | feature | subitem
tags: {area: 主车, module: 主车管理, capabilities: [], domain: generic}
status: 已发布
revision: {version: "3.4.0", state: approved, implements_iface: "1.0"}
interface: {version: "1.0", contract: "限制可添加的主车数量"}
presence: all              # default-on：全变体适用
parameter:                 # override 是结构化参数，不是自由文本
  name: max_ego_count
  type: "int | inf"
  by_cell:
    standalone: inf
    collab_client: inf
    collab_cloud: inf
    teach_standalone: 50
    research_standalone: TODO   # ⚠ 原表只写"是"，参数值待补
    teach_collab: TODO          # ⚠ 同上
dependencies: []
scope: 核心
---

## 契约
限制可添加的主车数量。`max_ego_count` 是参数而非开关。

## 验收
- 当 cell=teach_standalone，新增第 51 个主车被拒绝。 <!-- machine_checkable -->

## 备注
此功能即"单车/多车"的参数表达：`max_ego_count > 1` 即多车。原表无独立单/多车维度，由此参数派生（见 OQ1）。
